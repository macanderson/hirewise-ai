{
  "name": "api",
  "version": "0.1.0",
  "description": "FastAPI services",
  "scripts": {
    "dev": "PYTHONPATH=./src poetry run uvicorn api.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "PYTHONPATH=./src poetry run uvicorn api.main:app --host 0.0.0.0 --port 8000",
    "build": "poetry build",
    "test": "PYTHONPATH=./src pytest"
  }
}

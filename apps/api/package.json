{
  "name": "api",
  "version": "0.1.0",
  "description": "FastAPI services",
  "scripts": {
    "dev": "fastapi dev",
    "start": "fastapi run",
    "build": "fastapi build",
    "lint": "ruff check .",
    "format": "ruff format .",
    "test": "pytest"
  }
}

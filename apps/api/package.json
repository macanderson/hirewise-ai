{
  "name": "api",
  "version": "0.1.0",
  "description": "FastAPI services",
  "scripts": {
    "dev": "uvicorn src.api.main:app --reload",
    "start": "uvicorn src.api.main:app",
    "build": "poetry build",
    "lint": "ruff check .",
    "format": "ruff format .",
    "test": "pytest"
  }
}

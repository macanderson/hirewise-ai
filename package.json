{
  "private": true,
  "packageManager": "pnpm@10.11.0",
  "workspaces": [
    "apps/web",
    "apps/api",
    "packages/ui",
    "packages/eslint-config",
    "packages/typescript-config",
    "packages/lib",
    "packages/db"
  ],
  "devDependencies": {
    "@types/node": "^20.17.50",
    "@types/react": "^19.1.5",
    "@types/react-dom": "^19.1.5",
    "concurrently": "^9.1.2",
    "eslint-config-turbo": "^2.5.3",
    "turbo": "^2.5.3"
  },
  "prisma": {
    "schema": "./packages/db/schema.prisma"
  },
  "scripts": {
    "dev": "concurrently \"pnpm --filter web dev --turbopack\" \"cd apps/api && PYTHONPATH=./src poetry run uvicorn api.main:app --reload --host 0.0.0.0 --port 8000\"",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "prisma:generate:py": "cd packages/db && python -m prisma generate",
    "prisma:migrate:py": "cd packages/db && python -m prisma migrate dev",
    "prisma:generate:ts": "cd packages/db && npx prisma generate",
    "prisma:studio": "cd packages/db && npx prisma studio",
    "prisma:push": "cd packages/db && npx prisma push",
    "prisma:pull": "cd packages/db && npx prisma pull",
    "prisma:seed": "cd packages/db && npx prisma seed",
    "prisma:format": "cd packages/db && npx prisma format",
    "type-check": "pnpm -r run type-check",
    "test": "pnpm -r run test",
    "format": "concurrently \"pnpm --filter api format\" \"pnpm --filter web format\" \"pnpm --filter ui format\" \"pnpm --filter db format\"",
    "format:check": "concurrently \"pnpm --filter api format:check\" \"pnpm --filter web format:check\" \"pnpm --filter ui format:check\" \"pnpm --filter db format:check\""
  },
  "dependencies": {
    "typescript": "^5.8.3"
  }
}
